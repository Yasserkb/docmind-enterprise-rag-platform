package com.docmind.gateway.config;

import org.springframework.context.annotation.Configuration;

@Configuration
class RouteConfig {
    // Routes are declared in application.yml to keep local configuration transparent.
}
