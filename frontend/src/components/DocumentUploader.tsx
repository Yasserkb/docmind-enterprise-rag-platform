export function DocumentUploader() {
  return <div className="card"><h3>Document upload</h3><p>Use the Swagger endpoint <code>POST /api/v1/collections/{'{collection_id}'}/documents</code> or the demo seed button on the Home page.</p></div>;
}
