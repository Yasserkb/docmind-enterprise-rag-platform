# Local Demo

Start the project with Docker Compose, open `http://localhost:3000`, then click **Seed local demo data**. The seed endpoint creates a collection, indexes a sample policy document, runs one query and executes one evaluation run.

You can also call `POST /api/v1/demo/seed` from Swagger at `http://localhost:8000/docs`.
